<div
    title="${title}"
    data-id="${id}"
    data-piece="html"
    data-name="${name}"
    class="${class}"
    data-namespace="{{ $this->view->getNamespace() }}"
    data-view="{{ $this->view->getName() }}"
>
    ${context}
</div>
