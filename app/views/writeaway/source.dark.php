<div
    title="${title}"
    data-id="${id}"
    data-piece="source"
    class="${class}"
    data-name="${name}"
    data-namespace="{{ $this->view->getNamespace() }}"
    data-view="{{ $this->view->getName() }}"
>
    ${context}
</div>
